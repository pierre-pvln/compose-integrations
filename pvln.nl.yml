version: "2"

services: 

    mysqlserver:
        build: 
            context: ../services/rpi2/mysql_5.5
            dockerfile: Dockerfile
        ports:
        - "3306:3306"
        volumes:
        -  ${MY_MYSQL_DATA_VOLUME}:/var/lib/mysql
